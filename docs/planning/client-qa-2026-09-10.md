# Client Q&A — 2026-09-10

Source: `Question list.docx`, sent by the client (Ghaus Kayani) and relayed by Mehmoon,
2026-09-10. Preserved verbatim (source fidelity — see CLAUDE.md section 12) rather than
paraphrased, so future sessions can go back to the client's own wording. Extracted via
`unzip` + a throwaway Node script against `word/document.xml` (no `pandoc` on this
machine — same class of environment gap as the earlier "no real Python" note in CLAUDE.md
section 13; worth installing pandoc if `.docx` reading becomes routine).

A companion file, `chart of accounts.xlsx`, was sent alongside this document. Verified
cell-by-cell against the version already seeded in this repo (`src/db/schema/accounts.ts`,
`src/db/seed.ts`) — **exact match, no new content**. It is the same file/version already
logged as "version 2" in CLAUDE.md section 4, not a revision.

---

**Q: How many user roles do you want in the system? Please list the staff types (example:
cashier, manager, accountant, owner) and how many roles total.**

A: Counter Control / Corporate Control / Receipts/Payments Control / Inventory Control /
Management Control.

*(This is the "five-role" candidate already seeded in this repo's schema — see CLAUDE.md
sections 5.8/6. This answer is the closest thing to a direct client confirmation of that
list that exists so far.)*

---

**Q: For each role, what should they be allowed to do? For example, can a cashier see
purchase price, or only selling price? Can he give discount on his own or does he need
manager approval?**

A: It has to be flexible and I can choose or edit at any time.

*(Confirms the "runtime-configurable permissions" principle already assumed in CLAUDE.md
section 12 — but gives no actual default permission matrix. The specific questions asked
— cost visibility, discount approval — remain unanswered.)*

---

**Q: You mentioned Items/Markers Control Form and Control Part Form are separate things.
Can you explain what each one is used for, and how they connect to each other?**

A: Markers Control Form (first step) → Item Creation Form (dropdown from first step) →
Control Part Number (linking of different part numbers under one parent part number, and
also against which detailed information will be inserted applicable to all linked part
numbers).

*(Describes a three-step chain matching the structure already built —
`markers → items → control_parts` in `src/db/schema/inventory.ts` — rather than the
"Form 1A governs fields a–h of Form A" reading pulled from the handover doc, CLAUDE.md
section 5.2. See CLAUDE.md section 5.2 for the practical read on this.)*

---

**Q: When you add a new part in stock, what information do you write down for it?
(Example: part name, company, car model, price, quantity)**

A: As per item creation form. We can have detailed discussion again if you need be
according to forms formats.

*(Punts back to the Item Creation Form from the previous answer — doesn't add new field-
level detail beyond what's already in the handover doc's Form A spec, CLAUDE.md 5.1.)*

---

**Q: Your notes mention some reports but say "details to be continued". Can you confirm
the full and final list of reports you want to see? Also, you shared a chart of accounts
list separately, can you confirm if that list is final too, or is it still being worked
on?**

A: More focused on developing the structure. Reports would be for sure from same
structure. However, at this stage, bear with me for reports. Chart of accounts are final
but you should define coding in a way to create space for future expansion of it.
Moreover, though main account code is one but reports would be based on classification on
the basis of party form.

*(Two separate answers bundled: (1) reports list is still NOT final, deferred — the
three-way Reports/Vouchers conflict in CLAUDE.md section 8 is still open; (2) chart of
accounts IS confirmed final, with two explicit requirements — expansion-ready coding, and
report classification based on the Party Form. Both already existed as design notes in
CLAUDE.md section 4, now upgraded from assumption to direct client instruction.)*

---

**Q: For each report, who should be able to see it? (Example: only owner, or manager
too)**

A: Will link different reports to different groups as stated above.

*(Ties report visibility to the five role groups named in the first answer.)*

---

**Q: You are currently using a ready made generic system plus Excel files. Can you tell
us the name of that ready made system? Also please share some Excel files you use, so we
can see what data and format you currently work with.**

A: All is through system except that info is not structured. Moreover couldn't understand
what you are asking.

*(Did not resolve the migration-source open question — CLAUDE.md section 9's "Data
migration prerequisites unconfirmed" note. Client didn't understand the question as
phrased and no files were shared. Needs a simpler, more concrete follow-up — e.g. asking
for a screenshot of the current system, or a specific Excel file by name.)*
